# SVR-OCR Docs

This directory contains the authoritative planning documents for `SVR-OCR`.

## Source of truth

- `SOURCE_OF_TRUTH.md`
- `HEADER_FOOTER_IMPLEMENTATION_PLAN.md`

Use `SOURCE_OF_TRUTH.md` as the canonical reference for:

- method definition
- architecture
- verifier logic
- experiment plan
- implementation roadmap
- project decision rules

Use `HEADER_FOOTER_IMPLEMENTATION_PLAN.md` as the canonical implementation plan for improving the current `headers_footers` and `Absent` weaknesses in `SVR-OCR-Full`.

If future notes disagree with that file, update that file first.

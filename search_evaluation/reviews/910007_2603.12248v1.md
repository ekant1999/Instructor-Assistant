# Matching Features, Not Tokens: Energy-Based Fine-Tuning of Language Models

- paper_id: `910007`
- arxiv_id: `2603.12248v1`
- benchmark_category: `cs.LG`
- primary_category: `cs.LG`
- published: `2026-03-12T17:57:50Z`

## Abstract

Cross-entropy (CE) training provides dense and scalable supervision for language models, but it optimizes next-token prediction under teacher forcing rather than sequence-level behavior under model rollouts. We introduce a feature-matching objective for language-model fine-tuning that tar- gets sequence-level statistics of the completion distribution, providing dense semantic feedback without requiring a task-specific verifier or pref- erence model. To optimize this objective ef- ficiently, we proposeenergy-based fine-tuning (EBFT), which uses strided block-parallel sam- pling to generate multiple rollouts from nested prefixes concurrently, batches feature extraction over these rollouts, and uses the resulting embed- dings to perform an on-policy policy-gradient up- date. We present a theoretical perspective connect- ing EBFT to KL-regularized feature-matching and energy-based modeling. Empirically, across Q&A coding, unstructured coding, and transla- tion, EBFT matches RLVR and outperforms SFT on downstream accuracy while achieving a lower validation cross-entropy than both methods.

## Section Headings

- `front_matter`: Front Matter (pages 1-1)
- `abstract`: Abstract (pages 1-1)
- `introduction`: Introduction (pages 1-3)
- `language_modeling_with_feature_matching`: Language Modeling with Feature Matching (pages 3-6)
- `experimental_protocol`: Experimental protocol (pages 6-8)
- `results`: Experimental results (pages 8-10)
- `related_work`: Related Work (pages 10-10)
- `conclusion`: Conclusion (pages 10-13)
- `appendix`: Appendix Contents (pages 13-13)
- `the_feature_matching_loss_profile_and_its_optimal_behavior`: The feature-matching loss profile and its optimal behavior (pages 13-14)
- `feature_matching_with_whitening`: Feature matching with whitening (pages 14-14)
- `feature_matching_with_alignment_bias`: Feature matching with alignment bias (pages 14-14)
- `references`: References (pages 15-39)

## Search Notes

- Summary: Cross-entropy (CE) training provides dense and scalable supervision for language models, but it optimizes next-token prediction under teacher forcing rather than sequence-level behavior under model rollouts. We introduce a feature-matching objective for language-model fine-tuning that targets sequence-level statistics of the completion distribution, providing dense semantic feedback without requiring a task-specific verifier or preference model. To optimize this objective efficiently, we propose energy-based fine-tuning (EBFT), which uses strided block-parallel sampling to generate multiple rollouts from nested prefixes concurrently, batches feature extraction over these rollouts, and uses the resulting embeddings to perform an on-policy policy-gradient update. We present a theoretical perspective connecting EBFT to KL-regularized feature-matching and energy-based modeling. Empirically, across Q&A coding, unstructured coding, and translation, EBFT matches RLVR and outperforms SFT on downstream accuracy while achieving a lower validation cross-entropy than both methods.
- Use this file to curate exact topical queries, paraphrases, and expected pages/sections.
